sudo: required
dist: trusty
language: python
python:
    - '3.6'

cache:
    pip: true

before_install: cd faucet
install:
    - pip3 install -r requirements.txt
    - pip3 install -r test-requirements.txt
    - python3 setup.py -q sdist
script:
    - "python3 -m pytest --cov=. tests/"
