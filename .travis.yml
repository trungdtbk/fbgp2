sudo: required
dist: trusty
language: python
python:
    - '3.6'

cache:
    pip: true

install:
    - pip3 install -r requirements.txt
    - pip3 install -r test-requirements.txt
    - tests/install-faucet.sh

script:
    - "python3 -m pytest --cov=. tests/"
